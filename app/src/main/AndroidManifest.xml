<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.laundress2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RegisterMenu"
            android:label="@string/app_name" />
        <activity
            android:name=".RegisterLc"
            android:label="@string/app_name" />
        <activity
            android:name=".RegisterLhw"
            android:label="@string/app_name" />
        <activity
            android:name=".Login"
            android:label="@string/app_name" />
        <activity
            android:name=".ClientHomepage"
            android:label="@string/app_name" />
        <activity
            android:name=".ClientLaundryDetails"
            android:label="@string/app_name" />
        <activity
            android:name=".AddLaundryDetails"
            android:label="@string/app_name"
            android:theme="@style/MyDialog" />
        <activity
            android:name=".FindLaundryServiceProv"
            android:label="@string/app_name" />
        <activity
            android:name=".ChooseLaundryShop"
            android:label="Book New Request"></activity>
    </application>

</manifest>